# 📱 Study With Keshab - Android App সেটআপ গাইড

## ✅ সম্পন্ন হয়েছে:

### ১. ওয়েবসাইট আপডেট:
- ✅ `android-native-source/` ফোল্ডার তৈরি হয়েছে
- ✅ `MainActivity.java` - ওয়েবসাইট লোড করে
- ✅ `MyFirebaseMessagingService.java` - নোটিফিকেশন হ্যান্ডল করে
- ✅ `AndroidManifest.xml` - পারমিশন সেট করা
- ✅ `activity_main.xml` - Layout ফাইল
- ✅ `build.gradle.txt` - Gradle কনফিগারেশন

### ২. অ্যাডমিন প্যানেল আপডেট:
- ✅ `admin.html` - নতুন "Android App Notification" সেকশন যোগ হয়েছে
- ✅ `js/admin.js` - Android notification পাঠানোর ফাংশন যোগ হয়েছে

---

## 🚀 এখন আপনার করণীয়:

### ধাপ ১: Firebase Console সেটআপ

1. **Firebase Console-এ যান**: https://console.firebase.google.com
2. আপনার প্রজেক্ট সিলেক্ট করুন
3. বাম সাইডবার থেকে **"Project Overview"** এর পাশে ⚙️ (Settings) ক্লিক করুন
4. **"Project settings"** এ যান
5. নিচে স্ক্রল করে **"Your apps"** সেকশনে যান
6. **"Add app"** বাটনে ক্লিক করুন
7. **Android আইকন** সিলেক্ট করুন

### ধাপ ২: Android App রেজিস্ট্রেশন

Firebase-এ Android app যোগ করার সময়:

```
Android package name: com.studywithkeshab.app
App nickname (optional): Study With Keshab
Debug signing certificate SHA-1 (optional): [খালি রাখুন]
```

**"Register app"** বাটনে ক্লিক করুন।

### ধাপ ৩: google-services.json ডাউনলোড

1. Firebase থেকে **`google-services.json`** ফাইল ডাউনলোড করুন
2. এই ফাইলটি সংরক্ষণ করুন (পরে Android Studio-তে লাগবে)

---

## 📲 Android Studio সেটআপ

### ধাপ ১: নতুন প্রজেক্ট তৈরি

1. **Android Studio** খুলুন
2. **"New Project"** ক্লিক করুন
3. **"Empty Views Activity"** সিলেক্ট করুন → **Next**
4. নিচের তথ্য দিন:
   ```
   Name: Study With Keshab
   Package name: com.studywithkeshab.app
   Save location: [আপনার পছন্দের জায়গা]
   Language: Java
   Minimum SDK: API 21 (Android 5.0 Lollipop)
   ```
5. **Finish** ক্লিক করুন

### ধাপ ২: google-services.json যোগ করা

1. Android Studio-র বাম পাশে **"Project"** ভিউ সিলেক্ট করুন
2. **`app/`** ফোল্ডারে যান
3. আগে ডাউনলোড করা **`google-services.json`** ফাইলটি এখানে কপি করুন
4. ফাইল স্ট্রাকচার এরকম হবে:
   ```
   YourProject/
   ├── app/
   │   ├── google-services.json  ← এখানে
   │   └── src/
   ```

### ধাপ ৩: Gradle ফাইল আপডেট

#### A. build.gradle (Project level)

`build.gradle` (Project: Study_With_Keshab) ফাইল খুলুন এবং এই লাইনটি যোগ করুন:

```gradle
buildscript {
    dependencies {
        classpath 'com.google.gms:google-services:4.4.0'
    }
}
```

#### B. build.gradle (App level)

`build.gradle` (Module: app) ফাইল খুলুন এবং:

**উপরে plugins সেকশনে যোগ করুন:**
```gradle
plugins {
    id 'com.android.application'
    id 'com.google.gms.google-services'  // এই লাইনটি যোগ করুন
}
```

**dependencies সেকশনে যোগ করুন:**
```gradle
dependencies {
    // Firebase
    implementation platform('com.google.firebase:firebase-bom:32.7.0')
    implementation 'com.google.firebase:firebase-messaging:23.4.0'
    implementation 'com.google.firebase:firebase-analytics'
}
```

**"Sync Now"** ক্লিক করুন।

### ধাপ ৪: Java ফাইল কপি করা

#### A. MainActivity.java

1. `app/src/main/java/com/studywithkeshab/app/` ফোল্ডারে যান
2. `MainActivity.java` ফাইল খুলুন
3. আপনার প্রজেক্টের `android-native-source/MainActivity.java` এর কন্টেন্ট কপি করে পেস্ট করুন

#### B. MyFirebaseMessagingService.java

1. একই ফোল্ডারে **New → Java Class** ক্লিক করুন
2. নাম দিন: `MyFirebaseMessagingService`
3. আপনার প্রজেক্টের `android-native-source/MyFirebaseMessagingService.java` এর কন্টেন্ট কপি করে পেস্ট করুন

### ধাপ ৫: AndroidManifest.xml আপডেট

1. `app/src/main/AndroidManifest.xml` ফাইল খুলুন
2. `<manifest>` ট্যাগের ভিতরে (কিন্তু `<application>` এর বাইরে) এই পারমিশনগুলো যোগ করুন:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

3. `<application>` ট্যাগের ভিতরে এই সার্ভিসটি যোগ করুন:

```xml
<service
    android:name=".MyFirebaseMessagingService"
    android:exported="false">
    <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
    </intent-filter>
</service>
```

### ধাপ ৬: Layout ফাইল আপডেট

1. `app/src/main/res/layout/activity_main.xml` ফাইল খুলুন
2. আপনার প্রজেক্টের `android-native-source/activity_main.xml` এর কন্টেন্ট কপি করে পেস্ট করুন

### ধাপ ৭: Notification Icon যোগ করা

1. `app/src/main/res/drawable/` ফোল্ডারে যান
2. **New → Vector Asset** ক্লিক করুন
3. **Clip Art** থেকে একটি bell/notification আইকন সিলেক্ট করুন
4. নাম দিন: `ic_notification`
5. **Next → Finish**

---

## 🧪 টেস্ট করা

### ১. Build করুন:
- **Build → Make Project** (Ctrl+F9)

### ২. Run করুন:
- একটি Android ডিভাইস/এমুলেটর কানেক্ট করুন
- **Run → Run 'app'** (Shift+F10)

### ৩. নোটিফিকেশন টেস্ট:
1. অ্যাপ চালু করুন
2. আপনার ওয়েবসাইটের **Admin Panel** এ যান
3. **"Android App Notification"** সেকশনে যান
4. একটি টেস্ট মেসেজ লিখুন
5. **"Send to Android App"** বাটনে ক্লিক করুন
6. আপনার ফোনে নোটিফিকেশন আসবে! 🎉

---

## 📦 APK তৈরি করা

### Release APK:

1. **Build → Generate Signed Bundle / APK**
2. **APK** সিলেক্ট করুন → **Next**
3. **Create new...** ক্লিক করে একটি Keystore তৈরি করুন:
   ```
   Key store path: [আপনার পছন্দের জায়গা]/study-with-keshab.jks
   Password: [একটি শক্তিশালী পাসওয়ার্ড]
   Alias: studywithkeshab
   Validity: 25 years
   ```
4. **Next → release → Finish**
5. APK তৈরি হয়ে যাবে `app/release/` ফোল্ডারে

---

## 🎯 গুরুত্বপূর্ণ নোট:

### ✅ যা হয়েছে:
- ওয়েবসাইট অ্যাপে লোড হবে
- Firebase Topic: `all_android_users` এ সাবস্ক্রাইব হবে
- অ্যাডমিন প্যানেল থেকে সরাসরি Android ইউজারদের নোটিফিকেশন পাঠানো যাবে
- অ্যাপ বন্ধ থাকলেও নোটিফিকেশন আসবে

### 🔑 Server Key:
```
AIzaSyA_s7CWYwcKkcNIPoSJ5riuMwkixViHt-o
```

### 🌐 Website URL:
```
https://keshab1997.github.io/Study-With-Keshab/
```

### 📱 Package Name:
```
com.studywithkeshab.app
```

---

## ❓ সমস্যা হলে:

### Build Error হলে:
1. **File → Invalidate Caches → Invalidate and Restart**
2. **Build → Clean Project**
3. **Build → Rebuild Project**

### Notification না আসলে:
1. Firebase Console-এ Android app সঠিকভাবে যোগ হয়েছে কিনা চেক করুন
2. `google-services.json` ফাইল সঠিক জায়গায় আছে কিনা দেখুন
3. Internet permission আছে কিনা চেক করুন

---

## 🎉 সফল হলে:

আপনার অ্যাপ এখন:
- ✅ ওয়েবসাইট দেখাবে
- ✅ নোটিফিকেশন রিসিভ করবে
- ✅ অ্যাডমিন প্যানেল থেকে কন্ট্রোল করা যাবে

**শুভকামনা! 🚀**
