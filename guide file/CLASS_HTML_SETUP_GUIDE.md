# Class HTML Setup Guide (নতুন চ্যাপ্টারের জন্য)

## 📋 যখন নতুন চ্যাপ্টারের জন্য class1.html কপি করবে, তখন এই জিনিসগুলো পরিবর্তন করতে হবে:

---

## ✅ Step 1: HTML Head Section পরিবর্তন

### 🔹 Title পরিবর্তন করো (Line 6)
```html
<!-- পুরনো -->
<title>Ratio & Proportion - Class 01</title>

<!-- নতুন (উদাহরণ: Percentage চ্যাপ্টারের জন্য) -->
<title>Percentage - Class 01</title>
```

---

## ✅ Step 2: Header Section পরিবর্তন

### 🔹 Logo Path ঠিক আছে কিনা চেক করো (Line 48)
```html
<img src="../images/logo.jpg" alt="Logo" class="logo">
```
**নোট:** যদি তোমার নতুন চ্যাপ্টারে `images/logo.jpg` ফাইল থাকে, তাহলে এটা ঠিক আছে। না থাকলে logo ফাইল কপি করে নাও।

### 🔹 Chapter Name পরিবর্তন করো (Line 49)
```html
<!-- পুরনো -->
<h1>অধ্যায়: অনুপাত ও সমানুপাত (Ratio & Proportion)</h1>

<!-- নতুন (উদাহরণ: Percentage চ্যাপ্টারের জন্য) -->
<h1>অধ্যায়: শতকরা (Percentage)</h1>
```

---

## ✅ Step 3: Main Content পরিবর্তন

### 🔹 Class Number ঠিক আছে কিনা দেখো (Line 68)
```html
<h2>CLASS NO: 01</h2>
```
**নোট:** যদি class 2, 3, 4... এর জন্য কপি করো, তাহলে নম্বর পরিবর্তন করো।

### 🔹 Chapter Title আবার পরিবর্তন করো (Line 70)
```html
<!-- পুরনো -->
<h3><strong>অধ্যায়: অনুপাত ও সমানুপাত (Ratio & Proportion)</strong></h3>

<!-- নতুন -->
<h3><strong>অধ্যায়: শতকরা (Percentage)</strong></h3>
```

### 🔹 পুরো Content পরিবর্তন করো (Line 73 থেকে শেষ পর্যন্ত)
এখানে তোমার নতুন চ্যাপ্টারের:
- মৌলিক ধারণা
- সূত্র
- টেকনিক
- প্রশ্নের ধরন
- টিপস

সব লিখতে হবে।

---

## ✅ Step 4: CSS File Path চেক করো (Line 8)

```html
<link rel="stylesheet" href="css/style.css">
```

**নোট:** নিশ্চিত করো যে তোমার নতুন চ্যাপ্টারের `class` ফোল্ডারের ভিতরে `css/style.css` ফাইল আছে।

---

## 🎯 Quick Checklist (দ্রুত চেক করার জন্য)

যখন নতুন চ্যাপ্টারের জন্য class HTML কপি করবে, এই লিস্ট ফলো করো:

- [ ] **Title** পরিবর্তন করেছো? (Head section)
- [ ] **Header এর Chapter Name** পরিবর্তন করেছো?
- [ ] **Main Content এর Chapter Title** পরিবর্তন করেছো?
- [ ] **Class Number** ঠিক আছে?
- [ ] **Logo path** ঠিক আছে?
- [ ] **CSS file path** ঠিক আছে?
- [ ] **পুরো Content** নতুন চ্যাপ্টার অনুযায়ী লিখেছো?

---

## 📝 আমাকে কী বলতে হবে?

যখন নতুন চ্যাপ্টারের জন্য class HTML তৈরি করতে চাইবে, আমাকে এভাবে বলো:

```
"[Chapter Name] চ্যাপ্টারের জন্য class [number] HTML তৈরি করো"
```

**উদাহরণ:**
- "Percentage চ্যাপ্টারের জন্য class 1 HTML তৈরি করো"
- "Time and Work চ্যাপ্টারের জন্য class 2 HTML তৈরি করো"

অথবা আরো বিস্তারিত বলতে পারো:

```
"Percentage চ্যাপ্টারের জন্য class 1 HTML তৈরি করো। 
Content এ এই টপিকগুলো থাকবে:
- মৌলিক ধারণা
- সূত্র
- শর্টকাট টেকনিক
- প্রশ্নের ধরন"
```

---

## 🔥 Pro Tips

1. **Content লেখার আগে Planning করো:** কোন টপিকগুলো class 1 এ থাকবে, কোনগুলো class 2 এ থাকবে - এটা আগে ঠিক করো।

2. **Math Equation এর জন্য:** যদি fraction, square root, বা বিশেষ math notation লাগে, তাহলে inline style section এ (Line 10-35) প্রয়োজনীয় CSS আছে কিনা দেখো।

3. **Consistent Formatting:** সব class HTML এ একই ধরনের formatting রাখো (যেমন: h4 দিয়ে section heading, content-box class দিয়ে important content)।

4. **Logo/Images:** নতুন চ্যাপ্টারের `class` ফোল্ডারের পাশে `images` ফোল্ডার তৈরি করে logo রাখো।

---

## 📂 Folder Structure (মনে রাখো)

```
subject/Math/[Chapter-Name]/
├── class/
│   ├── class1.html  ← এই ফাইল
│   ├── class2.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── script.js
├── images/
│   └── logo.jpg
└── index.html
```

---

## ✨ সংক্ষেপে

**শুধু 4টা জিনিস পরিবর্তন করলেই হবে:**
1. **Title** (2 জায়গায়)
2. **Chapter Name** (2 জায়গায়)
3. **Class Number** (1 জায়গায়)
4. **Content** (পুরো body content)

বাকি সব (CSS, JS, structure) একই থাকবে! 🎉
