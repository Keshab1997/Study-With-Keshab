# Firebase Cloud Function Deployment Guide

## 🚀 Deploy করার পদ্ধতি

### Terminal এ এই commands run করুন:

```bash
# Firebase CLI install করুন
sudo npm install -g firebase-tools

# Firebase login করুন
firebase login

# Deploy করুন
firebase deploy --only functions
```

## ✅ Deploy হলে কাজ করবে!

Android notification সঠিকভাবে পাঠানো যাবে।
